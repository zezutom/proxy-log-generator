language: python
python:
  - "2.7"
install:
  - pip install -r requirements.txt
  - pip install coveralls

# run tests with coverage
script: coverage run src/log_generator_test.py

after_success: coveralls